const CalculatorLib = {
  adding(num: string, num2: string) {
    return Number(num) + Number(num2);
  },
  subtracting(num: string, num2: string) {
    return Number(num) - Number(num2);
  },
};
export default CalculatorLib;
